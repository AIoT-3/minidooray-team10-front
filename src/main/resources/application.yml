spring:
  application:
    name: teamProject

  cloud:
    gateway:
      mvc:
        form-filter:
          enabled: false  # HTML form 요청을 처리하는 필터 off (form body를 원본 그대로 전달)


  data:
    redis:
      host: localhost
      port: 6379
  session:
    redis:
      namespace: spring:session


